# ============================================================================
# FIXTURE contract — drug-exposure recruitment
# One of the step-0 representative requirements (plan.md §4). Passes the
# strict gate + registry conformance, and compiles against both example site
# bindings; the compiler tests assert both sites select the SAME patients on
# identically-seeded synthetic data.
# ============================================================================
project: 'Fixture: drug-exposure recruitment'
project_type: recruitment
target_n: ~20
schema_version: 3
cohorts:
- id: f4038ab4
  name: Exposed adults
  inclusion:
    id: 39a52600
    op: AND
    members:
    - id: 4fe755d6
      kind: demographic
      label: Adults
      source: demographics
      age_min: 18
      age_max: 80
    - id: b18b4e55
      op: OR
      label: Drug exposure (name or BNF)
      members:
      - id: f3314e36
        kind: codes
        label: By drug name
        source: prescribing
        drug_names:
        - metformin
      - id: 9897e28b
        kind: codes
        label: By BNF code
        source: prescribing
        bnf:
        - '06011'
  exclusions:
  - id: 9ddffb12
    kind: codes
    label: Prior condition
    source: hospital_admissions
    icd:
    - A00
